export default function Title({title}) {
  return (
    <section className="mt-20 h-30 flex flex-col justify-center items-center">
        <span className="text-4xl bbh-sans-bogle-regular text-green-800">{title}</span>
    </section>
  );
}
